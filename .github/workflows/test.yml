name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: self-hosted
    steps:
      - name: Say Hello
        run: echo "Hello It is working"
      - name: exiting now
        run: echo "test ran completed"